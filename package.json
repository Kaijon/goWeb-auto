{
  "name": "embedded-camera-ui",
  "version": "1.0.0",
  "description": "Embedded Camera Configuration Web UI",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "preview": "vite preview",
    "lint": "vue-tsc --noEmit"
  },
  "dependencies": {
    "vue": "^3.4.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "typescript": "^5.3.0",
    "vite": "^7.3.1",
    "vue-tsc": "^3.2.5"
  }
}
